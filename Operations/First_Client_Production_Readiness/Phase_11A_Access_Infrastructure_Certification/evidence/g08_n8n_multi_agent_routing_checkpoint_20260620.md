# Phase 11A n8n Multi-Agent Routing Checkpoint — 2026-06-20

Workflow inspected through n8n API in read-only mode.

## Workflow

- Name: `AgentFlow_AI_STAGE_C_Evolution_Inbound_Ingestion`
- ID: `8QAshjrkLrNF5kDI`
- Active: true
- Webhook path: `agentflow-stage-c-inbound`
- Settings: success data not saved; error executions saved

## Routing behavior observed

- Normalization extracts Evolution instance from payload fields including `instance`, `instanceName`, and `instanceId`.
- SQL generated by n8n resolves channel with:
  - `provider = 'evolution'`
  - `external_channel_id = <instance>`
  - `status = 'active'`
- Canonical RPC called: `public.ingest_inbound_message(...)`.
- If payload instance identity is missing, current code falls back to `AgentFlow_Primary`.

## Certification impact

- Multi-instance routing is structurally possible because n8n routes by Evolution instance name into `public.channels.external_channel_id`.
- Current fallback to `AgentFlow_Primary` is unsafe for Libertalia multi-agent onboarding.
- Before a Libertalia instance is created, unknown/missing instance identity should fail closed and produce error evidence, not route to the internal instance.

No secrets, API keys, tokens, QR contents, phone numbers, or private payloads were recorded in this checkpoint.
